var KeyFrameHelper = function(theItem) {
    var _KEYS = [];
    var _functions = [];
}

extend(BaseHelper, KeyFrameHelper);

if (typeof console !== 'undefined') {
    console.log('KeyFrameHelper loaded');
}
else if (debug instanceof Function) {
    debug('KeyFramerHelper loaded');
}
